<% if current_user.following?(user) %>
  <%= link_to image_tag('follow2.png',class:'follow-btn'),user_relationships_path(user.id), method: :delete %>
<% else %>
  <%= link_to  image_tag('follow1.png',class:'follow-btn'),user_relationships_path(user.id), method: :post %>
<% end %>