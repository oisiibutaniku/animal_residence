<div class="contents row">
  <p><%= @nickname %>さんの投稿一覧</p>
  <div class="pro_edit">
  <%= link_to 'プロフィールを編集', edit_user_path(@user.id) %>
  </div>
  <% @animals.each do |animal| %>
    <%= render partial: "animals/animal", locals: { animal: animal } %> 
     <% end %>
</div>